from __future__ import unicode_literals
from django.apps import AppConfig

class BetterresumeConfig(AppConfig):
	name = 'BetterResume'
	verbose_name = "Better Resume Profiles"

	"""def ready(self):
		from . import signals  # noqa"""
 